const countries = ['India', 'Germany', 'Dubai', 'United Kingdom', 'France'];

//exporting variable countries, then we can import it in other files

// In commonJS Environment we use module.exports instead of export
module.exports = countries;
